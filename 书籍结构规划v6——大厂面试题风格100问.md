# 《软件常识十万个为什么》v6 — 大厂面试题风格 · 100问清单

> v6 核心变化：所有问题重写为大厂系统设计面试题 / 深度追问 / 对比抉择风格。
> 标题 = 面试官会怎么问。正文开头 = 勾起好奇心的日常场景。
> 杜绝"为什么X很重要"这种空泛问法——每个问题都锚定在具体的技术场景和约束上。

---

## 风格标签说明

| 标签 | 含义 | 典型问法 |
|------|------|---------|
| [设计] | 系统设计面试题——给场景、给约束、让你从零设计 | "设计一个……，你会怎么做？" |
| [追问] | 深度追问——看似简单，追问下去能挖出好几层 | "从A到B，中间到底发生了什么？" |
| [抉择] | 对比抉择——A和B本质区别在哪？什么时候选A什么时候选B？ | "X vs Y，它们真正在trade off的什么？" |

---

## 第一章 | 系统基石（15问）

| # | 风格 | 问题 |
|---|------|------|
| 1 | [追问] | 按下电源键到桌面出现——CPU在这3秒内执行的指令序列是什么？BIOS/UEFI、Bootloader、Kernel、Init——每一步在做什么？为什么开机不能"一瞬间"？ |
| 2 | [追问] | 你写了一个Java程序，敲下`java HelloWorld`——从JVM启动到"hello world"打印出来，发生了什么？类加载→字节码验证→解释执行→JIT编译——同样是"运行一个程序"，JVM和操作系统直接加载二进制可执行文件（如Go编译出的ELF）有什么本质不同？ |
| 3 | [抉择] | 进程、线程、协程——三个"执行单元"，为什么Go用协程、Java用线程池、Nginx用多进程？各自的调度成本、内存开销、并发模型差异在哪？ |
| 4 | [追问] | 一次read()系统调用——从用户态到内核态，CPU切换了多少状态？为什么说系统调用"贵"？如果让你设计一个零拷贝方案，怎么绕开这个开销？ |
| 5 | [追问] | 虚拟内存——每个进程都以为独占4GB（32位）或256TB（64位），这个幻象怎么维持的？页表、TLB、缺页异常——一次内存访问如果TLB miss，要多花多少时间？ |
| 6 | [抉择] | 一个数组访问越界——Java抛出ArrayIndexOutOfBoundsException并打印完整堆栈，Python抛出IndexError，Go直接panic，而C语言可能静默地破坏相邻内存。同样是越界，为什么不同语言的处理方式差这么多？它们的数组在内存中的布局有什么不同导致了这种差异？ |
| 7 | [抉择] | 0.1 + 0.2 ≠ 0.3——这是IEEE 754浮点数的"特性"而不是bug。但如果你在写金融交易系统，怎么处理这个？BigDecimal vs 整数分（存"分"而不是"元"）vs 定点数——各有什么坑？ |
| 8 | [追问] | CPU的L1/L2/L3缓存和Redis缓存——都是"缓存"，一个硬件一个软件。为什么CPU缓存你不能靠写代码控制它的淘汰策略？它们的"透明性"差异决定了什么？ |
| 9 | [抉择] | SSD比HDD快一个数量级——但为什么SSD写入多了会掉速？GC（垃圾回收）和写入放大（Write Amplification）——设计一个日志系统（高频写入），怎么避免SSD提前报废？ |
| 10 | [追问] | `counter++`在多线程下为什么不是线程安全的？从Java字节码（getfield→iadd→putfield）到CPU的load→add→store三条指令——在中间被打断的后果是什么？AtomicInteger的CAS和synchronized分别是怎么解决这个问题的？Go的`sync/atomic`和Python的GIL又各自走了什么不同的路？ |
| 11 | [抉择] | "64位系统支持16EB内存"——但实际上你的电脑最多插192GB，服务器最多几TB。64位的真实瓶颈在哪？地址总线宽度、内存控制器、DIMM插槽——哪个才是真正卡你脖子的？ |
| 12 | [设计] | JVM的垃圾回收器（GC）——它怎么知道一块内存可以回收了？标记-清除、标记-复制、标记-整理——分别适合什么场景？为什么G1和ZGC要引入"分代"和"分区"的概念？如果让你给Go设计一个比现有三色标记更高效的GC，你会从Java的ZGC里抄什么？ |
| 13 | [追问] | 一个键盘按键被按下——从物理按键到屏幕上出现字符，经历了什么？中断控制器→CPU中断→键盘驱动→输入子系统→GUI事件循环→渲染——每一步在做什么？ |
| 14 | [抉择] | 大端（Big Endian）vs 小端（Little Endian）——x86是小端，网络字节序是大端。为什么这件事几乎只在处理网络协议和二进制文件时才跳出来烦你？为什么大家不统一？ |
| 15 | [设计] | 如果给你一台树莓派，插上一个LED灯，让你用Python写一段程序让它闪烁——你从哪开始？直接操作GPIO引脚是什么意思？内存映射I/O（Memory-Mapped I/O）是什么？为什么在树莓派上你可以用Python直接操作硬件，而在普通电脑上写Python却碰不到硬件？中间隔了什么东西？ |

---

## 第二章 | 网络协议（14问）

| # | 风格 | 问题 |
|---|------|------|
| 16 | [设计] | 如果让你从零设计一个"可靠传输协议"——你要解决：丢包怎么办？乱序怎么办？发送方太快接收方来不及处理怎么办？——TCP是怎么逐一解决这些问题的？ |
| 17 | [追问] | 你在浏览器输入baidu.com然后回车——从敲下回车到页面渲染，网络上到底发生了什么？DNS→TCP三次握手→TLS握手→HTTP请求→CDN→响应→浏览器渲染——每一步的延迟是多少？ |
| 18 | [抉择] | TCP vs UDP——不是"可靠vs不可靠"那么简单。实时音视频通话为什么选UDP？游戏为什么有时候用TCP有时候用UDP？QUIC（HTTP/3）基于UDP做了什么事让它"可靠"了？ |
| 19 | [追问] | DNS是一个全球分布式目录——一个域名解析请求是怎么在几百毫秒内完成的？根DNS→顶级域→权威DNS→本地缓存。如果根DNS挂了，互联网会怎样？真的挂过吗？ |
| 20 | [抉择] | HTTP/1.1 → HTTP/2 → HTTP/3——每一代在解决什么具体问题？队头阻塞从HTTP/1.1的应用层搬到了HTTP/2的TCP层，HTTP/3又怎么把它彻底消灭的？代价是什么？ |
| 21 | [追问] | HTTPS的"锁"图标——浏览器怎么验证这个网站是真的？证书链（根CA→中间CA→网站证书）+数字签名——如果有人伪造了一个证书，你的浏览器怎么识破？ |
| 22 | [抉择] | WebSocket vs HTTP长轮询 vs SSE（Server-Sent Events）——实时消息推送，三个方案分别在什么场景是最优解？为什么WebSocket在企业代理/防火墙环境下经常挂？ |
| 23 | [追问] | NAT——你家Wi-Fi下所有设备共享一个公网IP。外部服务器回包时，路由器怎么知道该转发给哪台设备？STUN/TURN又是怎么在P2P通信中穿透NAT的？ |
| 24 | [设计] | 设计一个CDN——你的视频需要在全球范围毫秒级加载。边缘节点用LRU还是LFU淘汰？什么时候回源？怎么预热热点内容？DNS智能调度怎么把用户导向最近的节点？ |
| 25 | [抉择] | 负载均衡算法——轮询、最少连接、IP哈希、一致性哈希——什么场景下选哪个？为什么"最少连接"在长短连接混合场景下反而可能不均衡？ |
| 26 | [追问] | 一条微信消息从北京发到纽约——光纤里跑的是光，光速是30万公里/秒，为什么端到端延迟通常是100-200毫秒而只有40毫秒的"光飞行时间"？中间的时间花在哪了？ |
| 27 | [抉择] | IPv4地址已经分配完了，IPv6天然解决这个问题——为什么二十多年了还没全面切换？双栈、NAT64、隧道——各种过渡方案的工程成本和谁在买单的博弈 |
| 28 | [追问] | "Wi-Fi满格却不能上网"——你的设备连上了路由器（二层），但路由器自己没连上互联网（三层）。DHCP在这个过程中做了什么？为什么有时候IP冲突会导致这个问题？ |
| 29 | [设计] | 设计一个P2P文件分发系统——像BitTorrent那样，下载的人越多越快。分片策略、Peer发现（Tracker/DHT）、激励机制（Tit-for-Tat）、稀有片段优先——这套机制怎么组合的？ |

---

## 第三章 | 架构设计（16问）

| # | 风格 | 问题 |
|---|------|------|
| 30 | [设计] | 设计一个短链接系统（像t.cn）——给定一个长URL生成短码，短码重定向回原URL。要求：短码全局唯一、每天生成百万级、重定向延迟<10ms。短码生成算法用什么（哈希？自增ID+62进制？），存储用SQL还是NoSQL，缓存怎么设计？ |
| 31 | [设计] | 设计一个千万级用户的登录系统——注册、登录、Token管理、多设备登录、单点登录（SSO）。Session vs JWT各自的优劣，OAuth2.0在什么环节介入？怎么防撞库？怎么防暴力破解？ |
| 32 | [设计] | 设计一个App的OTA升级系统——面向千万台设备，每次升级包500MB。怎么让升级流量不冲垮服务器（CDN+P2P分发）？怎么保证升级失败不"变砖"（A/B分区+回滚）？增量更新包怎么生成（bsdiff）？ |
| 33 | [设计] | 设计一个智能家居设备管理平台——100万个家庭的1000万台智能灯、空调、门锁。手机App发出"关灯"指令，灯恰好离线了——怎么保证它上线后执行这个指令？设备影子模型怎么实现？MQTT的QoS怎么配合？ |
| 34 | [设计] | 设计一个秒杀系统——100万人同时抢1000张票，你还会面对黄牛用脚本"比你快1000倍"、有人篡改请求参数、同一个IP注册100个账号。你的防线布在哪几层？IP限流→设备指纹→验证码（滑块/无感）→库存原子扣减——每一层在拦什么？ |
| 35 | [设计] | 设计一个消息队列——生产者、Broker、消费者。消息怎么保证不丢（ack + 持久化）？消息怎么保证不乱（partition + 顺序写）？消费者挂了重启怎么不丢进度（offset管理）？重复消费怎么处理（幂等）？ |
| 36 | [追问] | 缓存穿透、击穿、雪崩——这三件事看起来像但其实成因完全不同。穿透是查了不存在的数据、击穿是热点Key刚好过期、雪崩是大量Key同时过期。怎么布隆过滤器解决穿透而不能解决击穿？怎么互斥锁解决击穿？怎么随机TTL缓解雪崩？ |
| 37 | [抉择] | Redis为什么快——是"因为纯内存"吗？Memcached也用内存。Redis快在：单线程消除锁竞争、epoll IO多路复用、SDS/ziplist/skiplist手工优化数据结构、渐进式rehash——这些因素各自贡献了多少？ |
| 38 | [追问] | 分库分表——一张用户表到了10亿行，单库扛不住了。按什么字段分（user_id哈希）？拆完怎么跨库join（禁止join，靠应用层拼接）？怎么保证分布式事务（2PC/TCC/Saga）？怎么无感迁移？ |
| 39 | [设计] | 设计一个分布式ID生成器——不能靠数据库自增（单点故障），不能靠Redis（单点），要在几百台机器上各自独立生成全局唯一且趋势递增的ID。雪花算法（Snowflake）的64位分���是什么？时钟回拨怎么处理？ |
| 40 | [抉择] | 微服务vs单体——你们团队10个人维护了20个微服务，每次改一个小需求要改4个服务。"微服务把代码复杂度变成了运维复杂度"——这话对不对？什么规模的团队和业务量才真正需要微服务？ |
| 41 | [设计] | 设计一个支付系统——用户下单→支付→回调→退款→对账。支付回调延迟了怎么办（主动查询+回调双重确认）？用户付了两次怎么办（幂等+唯一订单号）？对账发现差了一分钱（长款/短款），怎么修？ |
| 42 | [设计] | 设计一个配置中心——线上服务需要动态修改配置（比如切换某功能开关），不能重启。怎么推送变更（长轮询 vs 实时push）？怎么灰度（先改1%的节点）？怎么回滚（保留历史版本）？Apollo/Nacos是怎么做的？ |
| 43 | [抉择] | 分布式事务——2PC（两阶段提交）简单但锁住资源太久，TCC（Try-Confirm-Cancel）灵活但业务代码要写补偿逻辑，Saga长事务拆成多个短事务+补偿——分别适合什么场景？为什么支付宝选择了TCC？ |
| 44 | [设计] | 设计一个API网关——前端请求先经过它。认证（验证JWT/OAuth Token）→限流（令牌桶/漏桶）→路由（转发到后端哪个微服务）→协议转换（前端RESTful，后端gRPC）。Kong/APISIX是怎么实现的？ |
| 45 | [设计] | 设计一个游戏的热更新系统——一个1GB的游戏资源包，需要在不重新下载整个App的情况下更新几张图片和一段脚本。差异更新（bsdiff）怎么生成几十MB的patch？iOS不允许动态加载代码，Lua/JS脚本方案怎么绕开？ |

---

## 第四章 | 安全攻防（20问）

| # | 风格 | 问题 |
|---|------|------|
| 46 | [抉择] | "哈希"能单向不可逆，"加密"能双向可解密，"签名"能证明"确实是我发的"——三件事本质不同，但新人常搞混。MD5、SHA-256（哈希）vs AES（加密）vs RSA签名——它们分别解决什么问题？ |
| 47 | [设计] | 设计一个密码存储系统——用户注册→存储密码→登录验证→改密码。要求：即使数据库被黑客完整拖走，他也不能反推出用户的明文密码。你会怎么存？为什么不能用MD5？盐值（Salt）怎么加？bcrypt/scrypt/Argon2的"计算成本因子"是什么？ |
| 48 | [追问] | HTTPS证书链——浏览器怎么知道一个网站"真的是ta声称的那个网站"？从根CA→中间CA→网站证书，信任是逐级"签"出来的。2011年荷兰CA DigiNotar被攻破——攻击者拿到了什么？为什么整个浏览器行业直接把它从信任链里踢出去了？ |
| 49 | [追问] | SQL注入——`SELECT * FROM users WHERE name='$input'`，如果input是`' OR '1'='1`——这条SQL变成了什么？为什么参数化查询（Prepared Statement）是唯一正确的解法？ORM就一定安全吗？ |
| 50 | [追问] | XSS攻击——一篇"精彩文章"里藏了一句`<script>send(document.cookie)</script>`——所有读过这篇文章的用户，Cookie都被偷走了。反射型、存储型、DOM型XSS的区别在哪？CSP（内容安全策略）怎么防御？ |
| 51 | [追问] | CSRF攻击——你登录了网银，Cookie还在。然后你点开了一个"萌猫图片"链接——这个链接里藏了一个隐形的表单，悄悄向网银发起转账请求。网银看到请求带着你的合法Cookie，以为是你本人的操作。SameSite Cookie是谁、用哪三个值、为什么是最优雅的防御？ |
| 52 | [抉择] | DDoS攻击为什么"防不住"——你花￥100租一小时僵尸网络，防御方需要花￥100万买清洗设备。这种不对称的本质是什么？流量清洗、Anycast分散、CDN吸收——各自的防御上限在哪？什么量级的DDoS（Tbps级）谁也扛不住？ |
| 53 | [设计] | 设计一个风控系统——你需要拦截：黄牛脚本抢票、羊毛党批量注册、刷单冲销量。规则引擎（"同一IP一分钟>100次→拦"）和机器学习模型（"这个用户行为轨迹不对劲"）各自负责什么？误杀（拦了真实用户）和漏杀（放了黄牛）怎么权衡？ |
| 54 | [抉择] | OAuth2.0的四种授权模式——授权码（最安全）、隐式（已淘汰）、密码（把自己的密码交给第三方）、客户端凭证（机器对机器）——它们分别适合什么场景？为什么隐式模式被淘汰了，PKCE是什么？ |
| 55 | [设计] | 设计一个SSO单点登录——登录了taobao.com，自动登录tmall.com。不同域名、Cookie不能跨域共享——怎么中转？CAS协议：用户→应用A→认证中心（登录）→返回ticket→应用A拿ticket去认证中心验证。OIDC（基于OAuth2.0的身份认证层）又比CAS好在哪？ |
| 56 | [追问] | 验证码的演变——从扭曲字符（OCR轻松破）→图文选择（CNN超过人类）→滑块（分析鼠标轨迹，真人会抖动）→无感验证（分析设备指纹+行为模式，不弹框）。AI每破一种，就要发明下一种。这场军备竞赛最终的出口在哪？ |
| 57 | [追问] | 侧信道攻击——不直接攻击算法，而是看算法执行时的"边角料"：CPU功耗波动（分析电流波形反推密钥bit）、执行时间差异（if分支耗​​时不同泄露信息）、电磁辐射。Spectre/Meltdown利用CPU的"推测执行"功能——推测执行的指令虽然结果被丢弃，但在缓存里留下了痕迹。怎么防御？ |
| 58 | [设计] | 设计一个接口防刷系统——一个秒杀接口，同一IP一秒100次请求、同一个手机号注册100个账号、同一个设备（Canvas指纹）换了100个IP——你要在网络层（IP频率限制）、业务层（设备指纹+行为分析）、数据层（下单频率异常检测）分别布什么防线？ |
| 59 | [抉择] | "记住密码"和"自动登录"——这两个功能让用户体验极好，但安全极差。Token存localStorage还是httpOnly Cookie？长期有效的Refresh Token如果被盗了怎么办？安全vs体验——这两个需求永远在拉扯，怎么在中间找平衡？ |
| 60 | [追问] | SIM Swap攻击——一个陌生人打电话给你的运营商客服："我手机丢了，请把号码转到我的新SIM卡上"。如果客服验证流程不严格——好了，你的手机没信号了，他收到了你的所有短信验证码。他不需要知道你的密码——"忘记密码"→"短信验证"→重置密码→登录成功。你该怎么防？ |
| 61 | [追问] | 撞库——A网站被"脱裤"泄露了email+密码MD5。攻击者拿着这一个组合去试B网站（你的邮箱）、C网站（你的网银）、D网站（你的微博）。如果你在不同网站用了相同密码——全没了。为什么说"密码复用"是所有个人安全漏洞里危害最大的？密码管理器是怎么解决这个问题的？ |
| 62 | [抉择] | "零信任" vs "边界防御"——传统安全是"进到内网就信任"，零信任是"永不信任，始终验证"。Google BeyondCorp怎么落地这个理念：所有访问经过统一代理、基于设备健康状态和用户身份动态授权、不再区分"内网"和"外网"。 |
| 63 | [设计] | 设计一个防暴力破解的登录接口——攻击者在试密码：同一IP试了10次→限IP，同一账户试了5次→临时锁定，试错间隔按指数退避（1秒→2秒→4秒）。但怎么防止攻击者用IP池绕开IP限制？怎么防止攻击者故意试错别人的账号把它锁了（DDoS账户）？ |
| 64 | [抉择] | 对称加密（你我用同一把钥匙）vs 非对称加密（我用你的公钥加密，你用自己的私钥解密）——为什么TLS不直接用非对称加密所有流量（因为慢1000倍）？为什么先非对称协商出对称密钥、再切对称？前向安全性（Forward Secrecy）是什么意思？ |
| 65 | [追问] | 被脱裤的数据——你去年在某个小论坛注册的账号+密码（MD5）被泄露了。到今天，这个组合还在被攻击者尝试用在你的微信、支付宝、邮箱上。你的密码管理器里的密码跟这个泄露的密码有关系吗？你会收到"异地登录"的提醒吗？ |

---

## 第五章 | 工程实践（12问）

| # | 风格 | 问题 |
|---|------|------|
| 66 | [设计] | 设计一个CI/CD流水线——代码push到Git→触发自动化：编译→静态检查→单元测试→集成测试→构建Docker镜像→推送到镜像仓库→部署到测试环境→部署到预发布→人工审批→发布生产。如果金丝雀阶段的p99延迟突然变高——怎么自动回滚？ |
| 67 | [抉择] | 测试金字塔——单元测试（多+快+隔离）→集成测试（中）→端到端E2E测试（少+慢+全链路）。为什么大量E2E+少量单元测试是灾难（CI跑2小时，开发跳过测试直接提交）？为什么90%的bug单元测试能抓到，但最后10%必须靠E2E？ |
| 68 | [抉择] | Git Rebase vs Merge——团队里永远吵的话题。Rebase把feature分支的commits搬过去，历史变一条线，但改写了SHA（历史是假的）。Merge创建一个Merge Commit，保留真实的并行开发线（历史是真的，但很乱）。什么场景该选哪个？"永远不要Rebase已经Push的公共分支"是为啥？ |
| 69 | [设计] | 设计一个发布系统——蓝绿部署（两套完整环境切换）成本高但秒级回滚。金丝雀发布（1%→5%→25%→100%）暴露面小但过程慢。灰度发布（Feature Flag按用户分组开放）最灵活但Flag本身也是一堆"if"。什么时候用哪种？它们能组合吗？ |
| 70 | [抉择] | 技术债——不是"烂代码"，是"为了更快上线故意简化了设计，后面要重构"。合理的债加速开发，烂债拖死整个团队。什么债务值得借（非核心业务、明确知道后期会重构）？什么债务绝对不能借（支付核心、安全相关、数据库schema设计）？ |
| 71 | [设计] | 设计一个监控告警系统——百万台服务器，每台每秒上报CPU/内存/磁盘/网络指标。你怎么聚合（Prometheus pull模型 vs 每台push到Kafka）？怎么判断"真挂了、还是网络抖一下"（连续N次超时才告警）？怎么防告警风暴（一个交换机挂了触发了1000条告警→合并成一条"交换机X挂了"）？ |
| 72 | [抉择] | 大厂为什么爱"造轮子"——不是"有Kafka了为什么还要自研消息队列"，而是Kafka的存储模型在你这个每秒一亿条消息的极端场景下变成了瓶颈。什么条件下"造轮子"是理性决策（差异化核心业务、规模超过现有方案的极限、外部方案无法满足合规）？什么条件下是"徒劳的"（非核心、规模小、成熟方案够用）？ |
| 73 | [抉择] | 单体vs微服务——不是"单体一定不好"。微服务把"代码的复杂度"转移成了"网络延迟+分布式事务+运维爆炸"。10人团队维护20个微服务——每次改一个小需求要改4个服务，你觉得这是在用火箭炮打蚊子吗？什么规模才真正需要拆微服务？ |
| 74 | [设计] | 设计一个RESTful API——不是说"用GET/POST就叫RESTful"。资源建模怎么设计（/users/123/orders vs /orders?userId=123）？分页（Offset vs Cursor，什么时候Cursor是唯一解）？错误码（业务错误码和HTTP状态码的分工）？幂等（相同的POST发两次，怎么保证不创建两个订单）？ |
| 75 | [抉择] | 软件基准测试为什么不准——你写了个for循环测性能，结果JIT给它优化没了（死代码消除），你测了个零纳秒。你测Java的启动速度，结果第一次JIT还没编译，慢得要死。你测MySQL的QPS，结果OS把整个表缓存在内存里了——你测的是OS的page cache不是MySQL。怎么控制这些变量？ |
| 76 | [抉择] | 需求文档为什么永远写不清楚——不是"没写清楚"，而是"你大脑里有大量你以为对方也知道的前提假设"。敏捷的答案是"不要试图一次性写清楚——用频繁交付暴露假设"。但敏捷≠没文档——接口契约、设计决策、状态机——什么文档是必须写的？什么可以靠代码+测试替代？ |
| 77 | [抉择] | 可观测性三支柱——Metrics（指标：CPU 90%）告诉你"有问题"、Tracing（链路追踪）告诉你"在哪段有问题"、Logging（日志）告诉你"具体什么问题"。三缺一有什么盲区？为什么Prometheus只覆盖了Metrics，要靠Jaeger和ELK补上Tracing和Logging？ |

---

## 第六章 | 分布式系统（9问）

| # | 风格 | 问题 |
|---|------|------|
| 78 | [抉择] | CAP定理——不是说"满足任意两个"，而是"网络分区发生时，在C（一致性）和A（可用性）之间二选一"。银行转账为什么选CP（宁可拒绝服务也不能出现余额不一致）？朋友圈为什么选AP（宁可短暂不一致也绝不能"加载失败"）？ |
| 79 | [设计] | 设计一个分布式锁——多个服务节点同时操作共享资源，只允许一个成功。Redis SETNX（死锁问题：锁过期了业务还没执行完→Redisson看门狗自动续期）vs ZooKeeper临时顺序节点（CP高可靠但性能差）。哪个场景该选哪个？ |
| 80 | [追问] | Raft共识算法——集群5个节点，Leader刚把一条日志复制到3个节点（多数）就挂了。新Leader选举出来后，怎么知道哪些日志已提交可以给客户端、哪些未提交应该丢弃？Leader的term和日志的index是怎么配合保证状态机绝对一致的？ |
| 81 | [抉择] | 容器vs虚拟机——Docker不是"轻量级虚拟机"，而是"带命名空间和Cgroups限制的普通进程"。它共享Host OS内核——所以不能在Linux上跑Windows容器。VM是硬件级虚拟化，完全隔离但重。什么场景下你宁愿用VM不用容器？ |
| 82 | [设计] | 设计一个分布式消息系统——百万QPS写入、消息不丢、不乱序。Kafka的答案：Producer按Key哈希分区→分区内顺序写（append-only log）→ISR（In-Sync Replica）确认→Consumer按分区拉取自己管offset。为什么Kafka不帮你管offset（让你自己管）？重复消费怎么处理？ |
| 83 | [设计] | 设计一个海量日志实时搜索系统——每秒TB级日志，要支持关键词搜索和聚合统计。Elasticsearch的倒排索引结构——但写入峰值超过indexing吞吐时怎么办（背压+限流）？时间序列数据为什么有人选ClickHouse（列存+压缩）而不是ES？ |
| 84 | [追问] | 熔断器（Circuit Breaker）——服务A调B调C，C开始超时→B的线程池全被C的请求占满→B也开始超时→A的线程池也被B占满→全链路雪崩。熔断器怎么在B发现"C连续失败3次"后→直接熔断→对A返回降级结果→保护A和B不被C拖下水？半开状态（Half-Open）的探针请求是什么？ |
| 85 | [设计] | 设计一个可横向扩展的实时计数器——微博热搜的"阅读量"、视频的"播放量"——同一篇文章可能被千万人同时阅读。怎么计数不丢不重？Redis INCR是单点瓶颈→分片计数器（sharded counter，按文章+时间窗口分片）→定期汇总。怎么做到"近似准确"（允许1%误差换100倍性能）？HyperLogLog是什么？ |
| 86 | [追问] | K8s为什么能"自愈"——一个Pod挂了，几秒后自动拉起来一个新的。这不是AI，是控制器循环（Reconciliation Loop）：Deployment控制器持续盯着"期望replicas=3 vs 实际replicas=2"→调用API创建一个新Pod。声明式API vs 命令式API——这个差异为什么彻底改变了运维的理念？ |

---

## 第七章 | 算法与智能（14问）

| # | 风格 | 问题 |
|---|------|------|
| 87 | [抉择] | 训练GPT这样的模型——为什么用GPU而不是CPU？CPU几十个大核适合复杂控制逻辑，GPU几千个小核适合海量并行矩阵运算。但推理（inference）不一定非用GPU——什么条件下CPU/TPU/NPU更适合推理？为什么有人用Mac跑70B大模型？ |
| 88 | [追问] | 梯度下降——损失函数+梯度+学习率。一个模型到底在"学"什么？想象你走在一个漆黑的山谷里，只能感受脚下的坡度——往坡度最陡的方向迈一步（梯度下降）。学习率太大→震荡甚至飞出谷底；太小→走不到底。Momentum（动量）和Adam（自适应学习率）怎么改进这个基本算法？ |
| 89 | [抉择] | 过拟合——训练集上完美，测试集上一塌糊涂。本质是：模型太复杂记住了噪声而不是规律。L1/L2正则化（惩罚大参数）、Dropout（随机丢弃神经元）、早停（验证集loss不再降就停）——分别在什么情况下最有效？为什么数据增强（旋转/裁剪图片）能从根上缓解？ |
| 90 | [追问] | 大模型"越大越聪明"——1B参数的模型不会写诗、不会推理、不会翻译芬兰语。100B的模型突然都会了——这些能力不是被训练的，是"涌现"的。缩放定律（Scaling Laws）说：loss随参数/数据/算力按幂律下降——为什么这个趋势看起来还没到天花板？ |
| 91 | [追问] | Self-Attention——Transformer为什么能取代RNN/LSTM成为所有大模型的基础架构？RNN必须逐个Token处理（无法并行、长距离依赖衰减）。Self-Attention让每个Token直接看所有Token——O(1)路径长度、GPU并行友好。代价是O(n²)计算复杂度——怎么用FlashAttention缓解？ |
| 92 | [抉择] | 推荐系统的冷启动——新用户没有行为数据，新商品没有互动数据。基于内容的推荐（看物品自身属性）→启动快但推荐同质化；协同过滤（"和你类似的人也喜欢"）→启动需要数据积累但推荐惊喜度高。混合策略怎么在前两周最大化留存？ |
| 93 | [抉择] | 监督学习（有标准答案：猫/狗）vs无监督学习（没有标签：自己找数据结构）vs强化学习（延迟奖励：下棋赢了+1分）——核心区别在"反馈信号是什么"。什么时候必须用强化学习（AlphaGo、自动驾驶决策）？为什么监督学习是最主流但在某些场景完全不适用？ |
| 94 | [追问] | 大模型"一本正经地胡说八道"——为什么不能在训练时直接禁止？因为模型本质是Next Token概率预测，没有"事实vs虚构"的内在判断。RAG（检索增强生成：先在知识库查到事实再生成）能缓解但无法根除。RLHF（人类反馈强化学习）让模型学会"不确定时说我不会"——能覆盖多少边界情况？ |
| 95 | [抉择] | 人脸识别vs图像分类——不是同一个问题。图像分类是"这张图属于猫/狗/鸟"（有限固定类别），人脸识别是"这个人是谁"（无限动态增加的类别）。度量学习（Metric Learning）——让同一人的两张照片在嵌入空间中距离更近——和Softmax分类有什么本质不同？ |
| 96 | [追问] | MapReduce——WordCount是它的"Hello World"，但MapReduce的思想远不止统计词频。Map（分）、Shuffle（整理）、Reduce（合）——为什么这个流水线能扩展到数千台机器？数据本地化（把计算搬到数据所在的机器）为什么是核心优化？Spark比MapReduce快在哪（内存中间结果 vs 全写磁盘）？ |
| 97 | [抉择] | 布隆过滤器——比哈希表省100倍空间，代价是可能误报（"可能存在"实际不存在）。但这个"宁可错报、绝不错过"的特性，在实际系统中有多好用？Google Chrome的安全浏览用布隆过滤器在本地检查URL是否恶意（几MB数据vs几百MB全量）。Redis缓存穿透也用布隆挡掉不存在的key——怎么配置位数组大小和哈希函数数量把误报率降到1%？ |
| 98 | [抉择] | 快速排序平均O(nlogn)最快但最坏O(n²)、堆排序最坏O(nlogn)但常数因子大、归并排序稳定但需要额外O(n)内存——三种排序各有优劣。C++ std::sort怎么把这三种混用（Introsort：快排递归过深→切堆排）？Timsort（Python/Java的默认排序）怎么利用"真实数据常有部分有序"的特性把这个混合做到极致？ |
| 99 | [抉择] | 哈希表O(1)查单点，B+树O(logn)但支持范围扫描和顺序遍历——为什么数据库索引用B+树而不是哈希表？（因为SQL大量查询是`WHERE age>20 AND age<30`这类范围扫描，哈希表只能查等于）。为什么Redis底层同时用了哈希表（存key-value）和跳表（存sorted set的分数排序）？ |
| 100 | [设计] | 设计一个推荐系统——"你可能喜欢"背后的四层漏斗。召回（从百万候选物中拿几千个可能的）→粗排（简单模型打分，从几千筛到几百）→精排（复杂模型精确排序）→重排（多样化、去重、打散）。冷启动、实时兴趣捕捉、多目标优化（点击率+停留时长+多样性）——每一层在做什么？每一层用什么算法？ |
